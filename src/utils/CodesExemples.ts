export const code1 = `return (
    <div className="h-screen flex items-center">
      <div className="bg-white shadow-md rounded">
        <h1 className="font-bold text-gray-800">
          Bem-vindo ao React com Tailwind CSS!
        </h1>
        <button className="hover:bg-blue-600">
          Clique Aqui
        </button>
      </div>
    </div>
  );`
export const code2 = `html, body, div, span, applet, object, iframe,h1, h2, h3, h4, h5, h6, p {  
  margin: 0;  
  padding: 0;  
  border: 0;  
  font-size: 100%;  
  font: inherit;  
  vertical-align: baseline;
}
table {  
  border-collapse: collapse;  
  border-spacing: 0;
}`